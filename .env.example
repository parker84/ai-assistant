# AI Assistant Configuration

# Google OAuth (Required for Calendar integration)
# Get these from Google Cloud Console: https://console.cloud.google.com
# 1. Create a project and enable Google Calendar API
# 2. Create OAuth 2.0 credentials (Web application)
# 3. Add http://localhost:8501 to Authorized redirect URIs
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8501

# LLM Provider Configuration
# Choose your provider: "anthropic" or "openai"
LLM_PROVIDER=anthropic

# Anthropic API Key (if using Anthropic)
# Get from: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API Key (if using OpenAI)
# Get from: https://platform.openai.com
OPENAI_API_KEY=sk-...

# Model Configuration
# Anthropic models: claude-sonnet-4-20250514, claude-3-opus-20240229, etc.
# OpenAI models: gpt-4, gpt-4-turbo, gpt-3.5-turbo, etc.
LLM_MODEL=claude-sonnet-4-20250514

# Timezone (for calendar events)
TIMEZONE=America/Toronto

# PostgreSQL Database (Required)
# Used for conversation history, tokens, knowledge base, reminders, and crucial events
# For local Docker Compose, these defaults match the postgres service:
POSTGRES_USER=assistant
POSTGRES_PASSWORD=assistant
POSTGRES_HOST=postgres
POSTGRES_DB=assistant

# Slack Integration (for /ea commands)
# Create a Slack app at https://api.slack.com/apps
# 1. Create new app → From scratch
# 2. Add Slash Command: /ea with URL: https://your-server.com/slack/commands
# 3. Install to workspace
# 4. Copy tokens below
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret

# Server settings (for Slack webhook)
PORT=8000
HOST=0.0.0.0

# Gmail SMTP (for sending emails)
# 1. Go to Google Account settings: https://myaccount.google.com/security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to App Passwords: https://myaccount.google.com/apppasswords
# 4. Generate an app password for "Mail"
# 5. Use your Gmail address and the generated app password below
GMAIL_ADDRESS=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password

# Daily Brief Scheduler
# USER_EMAIL: The email address to send daily briefs to (must have authenticated via web app first)
# BRIEF_HOUR: Hour to send daily brief (0-23, default: 8)
# BRIEF_MINUTE: Minute to send daily brief (0-59, default: 0)
USER_EMAIL=your-email@gmail.com
BRIEF_HOUR=8
BRIEF_MINUTE=0

# Telegram Bot (optional)
# Create a bot via @BotFather in Telegram, then set the token here.
# Users link their account by sending /start their@email.com (must have signed in via web app first).
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
